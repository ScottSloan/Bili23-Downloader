# WebUI 后端依赖（D8）
#
# **不进桌面发行版**：打包好的桌面安装包里没有这些包，`--web-ui` 在其中不可用，
# 启动时会给出明确提示。从源码运行或使用 Docker 镜像时才需要装。

fastapi==0.141.1
uvicorn[standard]==0.52.4
# aria2 的 JSON-RPC 走 WebSocket（D4）。uvicorn[standard] 会顺带装上它，
# 但我们是直接用的，显式声明以免上游哪天把它挪出 standard extra
websockets==17.1
