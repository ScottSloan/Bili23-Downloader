# WebUI 后端依赖（D8）
#
# **不进桌面发行版**：打包好的桌面安装包里没有这些包，`--web-ui` 在其中不可用，
# 启动时会给出明确提示。从源码运行或使用 Docker 镜像时才需要装。
#
# 版本先不钉死：S3-1 真正把 FastAPI 骨架搭起来之后再按实际用到的 API 固定版本，
# 现在钉一个没跑过的版本只是假的确定性。

fastapi
uvicorn[standard]
